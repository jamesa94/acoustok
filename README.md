# acoustok

Neural audio codec and tokenizer for audio language models.

Turn waveforms into discrete acoustic tokens with a convolutional encoder and
residual vector quantization, and reconstruct them with a matching decoder.

Work in progress.
